version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - npm ci
            - node console.log("this gets executed")
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: /public
        files:
          - '**/*'
      cache:
        paths: []
    appRoot: app