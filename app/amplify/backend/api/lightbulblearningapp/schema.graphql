type ChallengePool @model @auth(rules: [{allow: public}]) {
  id: ID!
  description: String
}

type OpenQuestionDraft @model @auth(rules: [{allow: public}]) {
  id: ID!
  questionText: String
  answerText: String
  challengePoolID: ID
  ChallengePool: ChallengePool @connection
}
 