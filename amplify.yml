version: 1
applications:
  - frontend:
      phases:
        prebuild:
          commands:
            - npm i
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: /public
        files:
          - '**/*'
      cache:
        paths: []
    appRoot: app
